function calculateSum(a, b) {
    // Debugger statement to pause execution
    debugger;
    
    let sum = a + b;
    console.log('Sum:', sum);
    return sum;
}

// Call the function
calculateSum(5,10);
